---
# Example ClawInstance with Telegram and Slack channels
apiVersion: kubeclaw.io/v1alpha1
kind: ClawInstance
metadata:
  name: alice
  namespace: default
spec:
  channels:
    - type: telegram
      secretRef: alice-telegram-secret
    - type: slack
      secretRef: alice-slack-secret
  agents:
    default:
      model: gpt-4o
      image: ghcr.io/alexsjones/kubeclaw/agent-codex:v0.0.25
      maxTokens: 128000
    sandbox:
      enabled: true
      image: ghcr.io/alexsjones/kubeclaw/sandbox:v0.0.25
      resourceLimits:
        cpu: "2"
        memory: 4Gi
    subagents:
      maxDepth: 3
      maxConcurrent: 5
  skills:
    - skillPackRef: k8s-ops
    - name: code-review
    - name: security-audit
  policyRef: default-policy
  authRefs:
    - provider: github
      secretName: alice-github-token
    - provider: openai
      secretName: alice-openai-key
